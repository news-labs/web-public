#!/usr/bin/env node
/**
 * Deprecated — staging provisioning removed (prod-only policy).
 *
 * Use: node scripts/provision-admin-web-prod.mjs [--apply-migrations]
 */
console.error(
  "provision-admin-web-stg.mjs is deprecated. Use provision-admin-web-prod.mjs instead.",
);
process.exit(1);
